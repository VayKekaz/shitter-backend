export class Env {
    environment: 'production' | 'development' = 'development';
    jwtSecret: string = 'nigger';

    postgresUser: string = 'shitter_backend';
    postgresPassword: string = '123';
    postgresDb: string = 'shitter';
    postgresHost: string = 'localhost';
    postgresPort: number = 5432;
}
